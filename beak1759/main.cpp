#include <bits/stdc++.h>
using namespace std;

int main()
{
    int l, c, i, j, k;
    scanf("%d %d", &l, &c);
    char arr[c+1];
    for( i=0; i<c; i++){
        scanf(" %c", arr[i]);
    }
    sort(arr+1, arr+1+c);
    for(j=0; j<)
    printf
}
