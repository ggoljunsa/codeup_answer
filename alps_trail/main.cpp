#include <bits/stdc++.h>
using namespace std;

sort(A+1; A+1+n);
sort(A+1, A+1+n, greater<int>());

sort(S+1, S+1+n; cmp)

int main()
{
    char arr[20];
    int l, c, i, j, k;
    scanf("%d %d", &l, &c);
    for( i=0; i<c; i++){
        scanf(" %c", arr[i]);
    }
    for( )
}
