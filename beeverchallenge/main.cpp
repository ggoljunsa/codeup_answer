#include <bits/stdc++.h>
using namespace std;


int main()
{
    int n , k;
    long long int rst = 1;
    scanf("%d %d", &n, &k);
    while(1)
    {
        if(k<1) break;
        if(k%2==1)
        {

        }
    }
    printf("%lld", rst);
    return 0;
}
