#include<bits/stdc++.h>
using namespace std;

int main()
{
    char a[100010];
    int b[2] = {};
    scanf("%s", a);
    int len = strlen(a);
    for(int i=0; i<len; i++){
        if(a[i]=='(') b[0]++;
        else if(a[i]==')')
    }

}
